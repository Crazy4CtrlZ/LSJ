fastapi>=0.115
uvicorn>=0.30
mcp>=1.2,<2  # 2.0 renamed FastMCP -> MCPServer with breaking stdio changes; project targets the 1.x SDK
openai>=1.40,<3  # 3.0 released mid-project and breaks our client usage; verified against 2.x (same lesson as mcp<2)
chromadb>=0.5
pypdf>=4.0
beautifulsoup4>=4.12
python-dotenv>=1.0
pydantic>=2.7
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
